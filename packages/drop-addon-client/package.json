{
  "name": "@drop-gse/addon-client",
  "version": "0.3.0",
  "private": true,
  "description": "Drop client addon: launch lifecycle hooks, VPN validation & UI for drop-gse",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/test/*.js"
  },
  "dependencies": {
    "@droposs/plugin-sdk": "^0.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.2"
  }
}
